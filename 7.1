using System;

namespace Assignment_bikas
{

    class Program
    {

        static unsafe void Main(string[] args)
        {
            Console.WriteLine("Enter a no of elements");
            int n = Int32.Parse(Console.ReadLine());
            int* arr = new int[n];
            for (int i = 0; i < n; i++)
            {
                arr[i] = int.Parse(Console.ReadLine());
            }
            for (int i = 0; i < n; i++)
            {
                Console.WriteLine("element[{0}] = {1}", i, arr[i]);
            }
        }
    }
}
